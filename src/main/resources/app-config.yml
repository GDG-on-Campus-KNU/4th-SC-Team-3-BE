pipy:
  storage:
    url: ${PIPY_STORAGE_URL}
  ai:
    url: ${PIPY_AI_URL}
    token: ${PIPY_AI_TOKEN}

spring:
  cloud:
    gcp:
      project-id: ${GOOGLE_PROJECT_ID}
      credentials:
        location: ${GOOGLE_KEY_PATH}
      storage:
        bucket: ${GOOGLE_STORAGE_BUCKET}