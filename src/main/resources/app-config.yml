pipy:
  storage:
    url: ${PIPY_STORAGE_URL}

spring:
  cloud:
    gcp:
      project-id: ${GOOGLE_PROJECT_ID}
      credentials:
        location: classpath:gcp-key.json
      storage:
        bucket: ${GOOGLE_STORAGE_BUCKET}